// Header.jsx
 // La funcion se llama igual que el componente

const Header = ({title}) => {
    return (
      <header>
        <h1>{title}</h1>
      </header>
    );
  };
  
  export default Header;